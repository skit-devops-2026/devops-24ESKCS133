apiVersion: v1
kind: Service
metadata:
  name: kisan-market
  labels:
    app: kisan-market
spec:
  selector:
    app: kisan-market
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
